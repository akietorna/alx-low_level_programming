#include "main.h"

/**
 * print_square - prints a square from hash
 * @size: the size of the square
 */

void print_square(int size)
{
	int a;

	if (n > 0)
	{
		for (a = 0; a < n; a++)
		{
			int b;

			for (b = 0; b < n; b++)
			{
				_putchar('#');
			}

			_putchar('\n');
		}
	}
	else
	{
		_putchar('\n');
	}
}
