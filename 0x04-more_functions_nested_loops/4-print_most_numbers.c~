#include "main.h"

/**
 * print_numbers - prints numbrs from 0 to 9
 */

void print_most_numbers(void)
{
	int c = '0';

	while (c <= '9')
	{
		if (c == 2 || c == 4)
		{
			c++;
		}
		else
		{
			_putchar(c);
			c++;
		}
	}
	_putchar('\n');
}
