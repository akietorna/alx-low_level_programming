#include "main.h"
#include <string.h>

/**
 *_strlen - prints the length of a string
 *strlen - gives length of a string
 *@s: takes in a string
 *
 *Return: Always 0
 */


int _strlen(char *s)
{
	int len = 0;

	while (*s[a] != '\0')
	{
		a++;
	}
	return (len + 1);
}
