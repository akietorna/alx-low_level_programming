#include "main.h"
#include <stdlib.h>

/**
 *_abs - compute absolute value of numbers
 *abs - computes the absolute value of numbers
 *@j: takes in number to be computered
 *Return: Always zero(success)
 */

int abs(int j);

int _abs(int j)
{
	return (abs(j));
}
