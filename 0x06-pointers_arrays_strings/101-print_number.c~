#include "main.h"

/**
 *print_number - prints an integer
 *@n: the number to be printed
 *
 *Return: Nothing
 */

void print_number(int n)
{
	while (n != 0)
	{
		if (n < 0)
		{
			n = -n;
		}
		_putchar((n % 10) + '0');
		n = n / 10;
	}
}
