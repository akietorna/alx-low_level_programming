#include <stdio.h>

/**
 * main - Entry point 
 *Description: 'prints a string'
 *@parameter: use the printf function to print 
 *
 *Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
