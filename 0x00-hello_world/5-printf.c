#include <stdio.h>
/**
 *main: Entry point
 *Description: prints a string
 *@void: expect nothing when the code runs
 *Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
